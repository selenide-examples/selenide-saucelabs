# selenide-saucelabs
Example of running Selenide tests on Saucelabs platform

## How to run tests:

> ./gradlew chrome
> ./gradlew firefox
> ./gradlew edge

## Prerequisites

You need to have two environment variables to run browsers on Saucelabs:
* `SAUCE_USERNAME` and 
* `SAUCE_ACCESS_KEY`
